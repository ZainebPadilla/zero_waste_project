<!DOCTYPE html>
<html>

<head>
  <title><%= content_for(:title) || "Zero Waste Project" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

  <%= favicon_link_tag 'logoeco2.png' %>
  <link rel="manifest" href="/manifest.json">
  <link rel="apple-touch-icon" href="/icon.png">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

</head>

<body>
  <nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container w-100 d-flex justify-content-between align-items-center">
      <div class="d-flex align-items-center">
        <%= image_tag "logoeco2.png", alt: "Logo", class: "navbar-logo" %>
        <%= link_to 'ECO²', root_path, class: 'navbar-brand ml-2' %>
        <!-- Nom de l'application avec marge à gauche -->
      </div>


      <!-- Bouton Hamburger (visible en mode mobile uniquement) -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarContent">
        <div class="mx-auto">
          <!-- Utilisation de mx-auto pour centrer le menu -->
          <ul class="nav justify-content-center">
            <li class="nav-item">
              <%= link_to 'Accueil', root_path, class: 'nav-link active', 'aria-current': 'page' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Team', pages_team_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Contact', pages_contact_path, class: 'nav-link' %>
            </li>
            <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Mon Dashboard #{current_user.first_name}!", dashboard_index_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Mes Productions', productions_path, class: 'btn btn-link nav-link' %>
            </li>
            <li class="nav-item">
              <%= button_to 'Déconnexion', destroy_user_session_path, method: :delete, class: 'btn btn-link nav-link' %>
            </li>
            <% else %>
            <li class="nav-item">
              <%= link_to 'Connexion', new_user_session_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Inscription', new_user_registration_path, class: 'nav-link' %>
            </li>
            <% end %>
          </ul>
  </nav>
  <div class="container mt-4;">
    <%= yield %>
  </div>

  <%= render 'layouts/footer' %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>