<!DOCTYPE html>
<html>

<head>
  <title><%= content_for(:title) || "Zero Waste Project" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <link rel="manifest" href="/manifest.json">
  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

</head>

<body>
<div class="page-wrapper">
  <nav class="navbar navbar-light bg-light">
  <div class="container w-100 d-flex justify-content-between align-items-center">
  <div class="d-flex align-items-center">
    <%= image_tag "logozerowaste.png", alt: "Logo", class: "navbar-logo" %>
    <%= link_to 'ZeroWaste', root_path, class: 'navbar-brand ml-2' %>  <!-- Nom de l'application avec marge à gauche -->
  </div>
  
     <div class="mx-auto"> <!-- Utilisation de mx-auto pour centrer le menu -->
          <ul class="nav justify-content-center">
          <li class="nav-item">
            <%= link_to 'Accueil', root_path, class: 'nav-link active', 'aria-current': 'page' %>
          </li>
      <li class="nav-item">
        <%= link_to 'Team', pages_team_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to 'Contact', pages_contact_path, class: 'nav-link' %>
      </li>
      <% if user_signed_in? %>
      <li class="nav-item">
        <span class="nav-link">Bonjour, <%= current_user.email %>!</span>
      </li>
      <li class="nav-item">
        <%= link_to 'Créer une production', new_production_path, class: 'btn btn-link nav-link' %>
      </li>
      <li class="nav-item">
        <%= button_to 'Déconnexion', destroy_user_session_path, method: :delete, class: 'btn btn-link nav-link' %>
      </li>
      <% else %>
      <li class="nav-item">
        <%= link_to 'Connexion', new_user_session_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= link_to 'Inscription', new_user_registration_path, class: 'nav-link' %>
      </li>
      <% end %>
    </ul>
  </nav>
    <div class="container mt-4">
    <%= yield %>
     </div>
    
    <%= render 'layouts/footer' %>
  </div>
</body>

</html>